# 拆解文档与技术方案模板

按此结构产出 `01-breakdown.md` 和 `02-tech-design.md`。方括号内为填写说明，产出时删除。

---

## 01-breakdown.md

```markdown
# <需求名称> 拆解文档

| 项 | 内容 |
|---|---|
| 需求来源 | <PRD 文件路径 / 链接> |
| 拆解日期 | <YYYY-MM-DD> |
| **拆解范围** | **只拆前端 / 只拆后端 / 前后端都拆**（来自第 0.5 步的用户确认） |
| 未拆的一端 | <如何处理：仅作为依赖列出 / 已给出契约 / 本次不涉及> |
| 状态 | 待确认 / 已确认，进行中 / 已完成 |
| 涉及端 | <服务端 / Web / iOS / Android / 小程序> |

## 一、需求概述

<用 3~5 句话说明这个需求要解决什么问题、给谁用、达成什么效果。不要复制 PRD 原文。>

## 二、项目现状摘要（Project Brief）

### 技术栈
<语言 / 框架 / 数据库 / 关键第三方库，一句话>

### 相关模块地图

| 模块 | 职责 | 关键路径 |
|---|---|---|
| <模块名> | <职责> | `src/xxx/` |

### 现有相似实现

| 需求点 | 现有实现 | 判断 |
|---|---|---|
| <R1 的相关功能> | `src/xxx/yyy.ts` | 复用 / 改造 / 新写 |

### 项目约定
- 目录与命名：<...>
- 接口风格：<...>
- 状态管理 / 样式方案：<...>
- 测试方式：<...>

### 约束与风险
<遗留代码、进行中的重构、必须兼容的版本、技术债>

## 三、需求清单

列出 PRD 中所有需求点，后续任务卡与此编号双向对应。

| 编号 | 需求点 | 验收标准 | 优先级 |
|---|---|---|---|
| R1 | <...> | <可观测的验收标准；PRD 未写则标注「待确认」> | 必须 / 应该 / 可选 |
| R2 | ... | | |

### 明确不做（Out of Scope）
- <PRD 中标注后续迭代的、或明显属于其他需求的>
- <本次不做的判断理由>

## 四、前后端职责切分

> 本节范围对应第 0.5 步的确认结果：
> - **只拆前端 / 只拆后端时，本表仍要完整填写**——因为「不拆的那一端要做什么」正是本端能否开工的依据，跳过它就会出现没人知道的阻塞项
> - 只拆一端时，在表下补充「另一端依赖清单」

逐条需求判定归属。**「前端不处理」列必须填写**——它声明的是排除项，防止业务逻辑被顺手写进组件。
判定规则见 `references/frontend-backend-split.md`。

| 需求 | 前端职责 | 后端职责 | 前端不处理（明确排除） | 归属结论 |
|---|---|---|---|---|
| R1 | <...> | <...> | <...> | 后端主导 / 前端主导 / 后端供数前端展示 / 纯前端 / 纯后端 |

### 另一端依赖清单（只拆一端时必填）

**只拆前端时**——列出前端开工前必须拿到的接口：

| 需求 | 需要调用的接口 | 需要的字段 | 现状 | 阻塞的前端任务 |
|---|---|---|---|---|
| R1 | `POST /api/orders/{id}/cancel` | `canCancel`, `cancelDisabledReason` | 已存在，见 `internal/router/router.go:42` | T006 |
| R2 | `GET /api/orders/{id}/timeline` | `items[].status`, `items[].at` | **不存在，待后端提供**，期望契约见第五节 | T007 |

**只拆后端时**——列出本次必须交付给前端的契约：

| 需求 | 交付契约 | 契约位置 | 前端可否据此开工 |
|---|---|---|---|
| R1 | `GET /api/orders/{id}/timeline` 完整定义 | `02-tech-design.md` 第三节 | 是（契约冻结即可并行） |

### 接口契约清单

前后端任务的连接点。契约在此定稿后，两端才能并行开发。

| 契约 | 方法/路径 | 提供方 | 消费方 | 对应任务 |
|---|---|---|---|---|
| <时间线查询> | `GET /api/orders/{id}/timeline` | 后端 T003 | 前端 T006 | T003 / T006 |

### 职责切分自查

- [ ] 没有前端自行判断业务规则（应消费后端的 `canXxx` 标记）
- [ ] 权限判定在后端，前端只控制显隐
- [ ] 金额 / 库存 / 积分计算全部在后端
- [ ] 分页列表的搜索与排序在后端
- [ ] 校验规则单一来源，前后端一致且有测试覆盖
- [ ] 同时涉及两端的需求已拆成前端卡 + 后端卡
- [ ] **若只拆一端：另一端依赖清单已填写，且标明了哪些接口尚不存在**

## 五、缺口与待确认问题

### 已确认

| 问题 | 结论 | 确认日期 |
|---|---|---|
| Q1 <问题> | <用户确认的答案> | <日期> |

### 待确认

| 问题 | 现状 | 推荐方案 | 备选 | 影响 |
|---|---|---|---|---|
| Q2 <问题> | <代码调研结论 + 文件路径> | <推荐> | <备选> | <阻塞哪些任务> |

## 六、拆分总览

### Epic / Feature 结构

按端标注，同一 Feature 下的前后端任务分列：

- **E1 <Epic 名>**
  - F1.1 <Feature 名>
    - T001 [后端] <任务标题>
    - T002 [前端] <任务标题>
  - F1.2 ...

### 需求 → 任务映射

| 需求 | 前端任务 | 后端任务 |
|---|---|---|
| R1 | T002 | T001, T003 |
| R2 | T005 | T004 |

### 按端汇总

| 端 | 任务数 | 工作量合计 | 备注 |
|---|---|---|---|
| 前端 | <n> | <S/M/L 合计> | <...> |
| 后端 | <n> | <...> | <...> |
| 联调 | <n> | <...> | <...> |

## 七、排期与依赖

### 依赖图

```
T001 契约定义 ──┬──> T002 [后端] 实现 ──┐
                └──> T003 [前端] 对接 ──┼──> T005 联调
T004 [前端] 样式 ────────────────────────┘
```

### 可并行分组

| 组 | 任务 | 说明 |
|---|---|---|
| G1 | T002 [后端], T003 [前端] | 契约已定，前后端可并行 |

### 里程碑

| 里程碑 | 内容 | 可验证产出 | 包含任务 |
|---|---|---|---|
| M1 | 契约冻结 | 接口字段/枚举/错误码评审通过，文档定稿 | T001 |
| M2 | 接口可用 | 接口返回预期数据，Postman/curl 可见 | T002 |
| M3 | 前端对接完成 | 页面数据真实渲染，异常态表现正确 | T003, T005 |

### 关键路径

<哪条链路决定整体工期，风险点是什么>

### 工作量分级

| 任务 | 估计 | 理由 |
|---|---|---|
| T001 | M | <为什么是 M> |

> S = 半天内，M = 1~2 天，L = 3~5 天。L 以上应继续拆分。
```

---

## 02-tech-design.md

```markdown
# <需求名称> 技术方案

## 一、整体设计

<一段话说明技术方案的主思路：新增什么、改造什么、数据怎么流。>

### 架构/流程示意

```
<用简单的 ASCII 图或步骤列表说明数据流转，不要画复杂的 UML>
前端请求 → 新接口 → 服务层 → 新表 → 返回
```

## 二、数据模型变更

### 表结构

| 表 | 变更类型 | 字段 | 类型 | 说明 | 是否迁移历史数据 |
|---|---|---|---|---|---|
| orders | 新增字段 | status_v2 | tinyint | 新状态枚举 | 是，需回填 |

### 迁移方案

- 迁移脚本：`migrations/xxxx_xxx.sql`
- 历史数据处理：<回填方式、执行时机、预估数据量>
- 回滚方案：<如何回退>

### 枚举/常量

| 名称 | 取值 | 含义 |
|---|---|---|
| OrderStatus | 0/1/2/3/4 | <...> |

## 三、接口契约

> **这是前端任务与后端任务的唯一连接点，全文只在此定义一次。** 任务卡引用本节，不复制、不重新定义字段名。
> 契约先冻结，前后端才能并行开发。

### GET /api/orders/{id}/timeline

| 项 | 内容 |
|---|---|
| 提供方 / 消费方 | 后端 T002 / 前端 T003 |
| 用途 | <...> |
| 鉴权 | <...> |
| 请求参数 | `id` (path, string, 必填) |
| 响应 | 见下方示例 |
| 错误码 | 404 订单不存在 / 403 无权限 |
| 分页 | <是/否，字段形式> |
| 排序 | <由后端保证的顺序，前端不重排> |
| 幂等 | <是否幂等，幂等键形式> |

```json
{
  "code": 0,
  "data": {
    "items": [
      { "status": 1, "canCancel": false, "cancelDisabledReason": "订单已发货" }
    ]
  }
}
```

**可操作性标记**：接口返回 `canCancel` / `canEdit` / `canDelete` 这类标记，前端据此渲染按钮的可用与禁用——前端不自行推导业务规则，规则变更只改后端一处。

### 契约变更记录

| 日期 | 变更 | 影响端 | 已通知 |
|---|---|---|---|
| <...> | <...> | 前端 / 后端 | <...> |

## 四、前端/客户端改动

<页面、组件、状态管理、路由、请求封装、交互细节（loading / 空态 / 错误态）>

### 前端明确不处理

<列出前端刻意不做判断的部分，指明它改为消费后端的哪个字段。这一节直接来自拆解文档的职责切分表，防止开发者顺手把业务逻辑写进组件。>

- 不做「能否取消」的判断 → 消费接口的 `canCancel`
- 不前端拼接金额 → 直接展示后端返回的 `totalAmount`（含精度与符号）
- 不硬编码状态含义 → 使用后端返回的 `statusText` / 字典接口

## 五、关键实现要点

<容易踩坑的地方：并发、事务边界、缓存失效、幂等、分页、权限校验的位置>

**权限校验必须明确写在后端接口层**，并注明参照哪个既有中间件/装饰器；不要写成「前端控制入口」。

## 六、风险与备选方案

| 风险 | 影响 | 应对 |
|---|---|---|
| <...> | <...> | <...> |

## 七、测试要点

<需要覆盖的异常路径与边界：空数据、超长输入、并发、越权、重复提交>
```
